import {isLoggedIn,logout} from './api.js';
export function mountChrome(){const header=document.querySelector('#site-header'),footer=document.querySelector('#site-footer');if(header)header.innerHTML=`<nav class="nav" aria-label="Main navigation"><a class="brand" href="/"><span>🐾</span>Pawfect Home</a><button class="menu" aria-label="Toggle menu" aria-expanded="false">☰</button><div class="nav-links"><a href="/#pets">Adopt</a><a href="/#about">About</a>${isLoggedIn()?'<a href="/pet/create.html">Add a pet</a><button class="link-button" id="logout">Log out</button>':'<a href="/account/login.html">Admin login</a>'}</div></nav>`;if(footer)footer.innerHTML=`<div class="footer-inner"><a class="brand brand-light" href="/"><span>🐾</span>Pawfect Home</a><p>Connecting good people with pets who need them.</p><small>© ${new Date().getFullYear()} Pawfect Home · Adopt responsibly.</small></div>`;header?.querySelector('.menu')?.addEventListener('click',e=>{const links=header.querySelector('.nav-links'),open=links.classList.toggle('open');e.currentTarget.setAttribute('aria-expanded',open)});header?.querySelector('#logout')?.addEventListener('click',()=>{logout();location.href='/'})}
export function petCard(p){return `<article class="pet-card"><a href="/pet/?id=${encodeURIComponent(p.id)}" aria-label="View ${escapeHtml(p.name)}"><div class="card-image"><img src="${safeUrl(p.image?.url)}" alt="${escapeHtml(p.image?.alt||p.name)}" loading="lazy"><span class="status ${p.adoptionStatus.toLowerCase()}">${escapeHtml(p.adoptionStatus)}</span></div><div class="card-body"><div><h3>${escapeHtml(p.name)}</h3><p>${escapeHtml(p.breed)}</p></div><span class="arrow" aria-hidden="true">↗</span><ul><li>${escapeHtml(String(p.age))} ${p.age===1?'year':'years'}</li><li>${escapeHtml(p.gender)}</li><li>${escapeHtml(p.size)}</li></ul><p class="location">⌖ ${escapeHtml(p.location)}</p></div></a></article>`}
export const escapeHtml=value=>String(value??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
export const safeUrl=value=>{try{const u=new URL(value);return ['http:','https:'].includes(u.protocol)?u.href:''}catch{return ''}};
export function toast(message){const t=document.createElement('div');t.className='toast';t.setAttribute('role','status');t.textContent=message;document.body.append(t);setTimeout(()=>t.remove(),2800)}
