import {authenticate} from './api.js';import {mountChrome} from './ui.js';mountChrome();const form=document.querySelector('#auth-form'),mode=location.pathname.includes('register')?'register':'login';form.addEventListener('submit',async e=>{e.preventDefault();const button=form.querySelector('button'),error=document.querySelector('#form-error');button.disabled=true;error.textContent='';try{await authenticate(mode,Object.fromEntries(new FormData(form)));const next=new URLSearchParams(location.search).get('next');location.href=next&&next.startsWith('/')?next:'/'}catch(err){error.textContent=err.message;button.disabled=false}});
